import pandas as pd

class PriceFactorsBuilder:
    """Placeholder builder for price related features."""
    def build(self, df: pd.DataFrame) -> pd.DataFrame:
        # Return an empty DataFrame with expected columns (none for now)
        return pd.DataFrame()
