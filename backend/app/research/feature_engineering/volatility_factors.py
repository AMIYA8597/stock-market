import pandas as pd

class VolatilityFactorsBuilder:
    """Placeholder builder for volatility related features."""
    def build(self, df: pd.DataFrame) -> pd.DataFrame:
        return pd.DataFrame()
